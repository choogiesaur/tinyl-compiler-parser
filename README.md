# tinyl-compiler-parser
tinyL machine code compiler and recursive descent parser for Principles of Programming Languages. my code is in Compiler.c

Usage:

$ make

$ ./compile <testfile> //test programs located in 'tests' directory

$ ./run tinyL.out
